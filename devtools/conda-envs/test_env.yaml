name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - configargparse
  - conda-forge::openbabel

    # Testing
  - coverage
  - flake8 
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - codecov
    - seamm
    - seamm-util
    - seamm-widgets

    # Testing
    - yapf
