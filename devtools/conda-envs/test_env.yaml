name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - configargparse
  - conda-forge::openbabel

    # Testing
  - coverage
  - flake8 
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - codecov
    - seamm>=0.9
    - seamm-util>=0.9
    - seamm-widgets>=0.9

    # Testing
    - yapf
