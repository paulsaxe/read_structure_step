name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - conda-forge::openbabel

    # Testing
  - pytest
  - pytest-cov
  - flake8 

    # Pip-only installs
  - pip:
    - codecov
    - yapf
